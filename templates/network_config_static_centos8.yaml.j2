network:
  version: 1
  config:
    - type: physical
      name: eth0
      subnets:
        - type: static
          address: {{ vm_ip }}/{{ vm_prefix }}
          gateway: {{ vm_gw }}
          dns_nameservers: [{{ vm_dns }}]
